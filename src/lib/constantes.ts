/** Taxa de entrega padrão de teste (R$) — o dono configura isso na Fase 6 */
export const TAXA_ENTREGA_PADRAO = 8;

/** Pedido pago em "novo" sem aceite da loja (alerta dono) */
export const SLA_NOVO_MINUTOS = 5;

/** Pedido "pronto" sem entregador atribuído (alerta dono) */
export const SLA_PRONTO_SEM_ENTREGADOR_MINUTOS = 8;
